#include "main.h"
#include <stdio.h>
/**
*main - prints the alphabet in lowercase, with prototype "main.h"
*
*Return: Aways 0
*/
int main(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
		printf("%i", i);
		printf("\n");
return (0);
}
